name: Temp auth mail UX 2
on:
  push:
    paths:
      - '.github/workflows/temp-auth-mail-ux2.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/patch_auth_mail_ux.py
      - run: |
          git config user.name "lunchpack-arg"
          git config user.email "pasalochevere@gmail.com"
          git add portal-v2/index.html
          git commit -m "P2.12C.7C-2 improve magic link resend UX"
          git push
